---
// Breadcrumb.astro - 面包屑导航组件
export interface Props {
  items: { label: string; href?: string }[];
}

const { items } = Astro.props;
---

<nav class="mb-6 text-sm font-body" aria-label="Breadcrumb">
  <ol class="flex items-center flex-wrap gap-2 rounded-full border border-ink/10 bg-white/70 px-4 py-2 shadow-[0_8px_20px_rgba(26,26,26,0.04)] backdrop-blur-sm">
    <li>
      <a href="/" class="text-gray-500 hover:text-ochre transition-colors">Home</a>
    </li>
    {items.map((item, index) => (
      <>
        <li class="text-gray-300">/</li>
        <li>
          {item.href ? (
            <a href={item.href} class="text-gray-500 hover:text-ochre transition-colors">
              {item.label}
            </a>
          ) : (
            <span class="text-ink font-medium">{item.label}</span>
          )}
        </li>
      </>
    ))}
  </ol>
</nav>
